# Default values for gcc.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: gcr.io/cnrm-eap/controller
  tag: 08b0467
  pullPolicy: IfNotPresent

nameOverride: ""
fullnameOverride: ""

# precreated secret containing gcp credentials
secretName: gcp-key

# project to watch
projectID: pgtm-pczarkowski